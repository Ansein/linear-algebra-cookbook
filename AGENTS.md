# AGENTS.md — 仓库规则

> 本文件是本仓库的**唯一权威规则来源**。任何 agent（人或 AI）在本仓库动手前必须先读本文件。
> 规则变更必须改这里，不要只写在聊天里。

---

## 1. 项目目标

把本科**高等代数**教材/教辅的知识，按**考研数学线性代数大纲**的范围重新整合，
产出一份 **LaTeX 编译的 PDF 考研蓝本**。

- 蓝本主结构 = 《线性代数9讲》（张宇，2027版）
- 范围边界 = 《全国硕士研究生招生考试数学考试大纲》
- 深化材料 = 北大《高等代数》第五版 + 樊启斌《高等代数典型问题与方法》

**一句话**：以考研大纲为边界，以 9 讲为骨架，用高等代数的视角把每一块知识讲透。

---

## 2. 两条已锁定的核心决定（2026-xx，用户确认，不得擅自更改）

### 决定一：产出形态 = **整合型考研蓝本**，不是全量重排
不是把《线性代数9讲》121 页原样数字化，而是：

- 按**考纲范围 + 9 讲知识点正文**重新组织
- **例题精选**：保留有代表性的、能体现方法的；剔除纯重复练习
- 目标是「一份读得完、能直接用于考研复习的蓝本」，而不是「一本书的扫描电子版」

> 后果：内容取舍是**允许且必要**的。但取舍必须在 `OUTLINE.md` 里逐条记录理由。

### 决定二：高等代数补充的深度上限 = **只补能加深理解、反哺解题的深化知识**
- ✅ 补：能让考纲内知识**从"会算"升级到"懂为什么"**的内容
  （例：特征空间/特征子空间、Jordan 块为何不可相似对角化、最小多项式、
  相似不变量、秩的等价刻画、基变换的几何意义……）
- ❌ 不补：纯理论拓展、考研完全不沾的内容
  （例：多项式理论、λ-矩阵完整理论、欧氏空间算子理论、双线性型与辛空间……）

**判定标准（每条补充都要能过这一关）**：
> 这条深化知识，是否能让考生**更快、更准、或更不容易错地**解出考纲内的题？
> 答不上来的，就不补；答得上来的，还要标注它对应考纲的哪个知识点。

每一条补充必须标注：`【深化】` + 它服务的考纲知识点 + 一句话说明"反哺点"。

---

## 3. 已知事实（已探查，勿重复劳动）

### 3.1 源文件（全部为纯图片扫描件，**无任何文字层**）

| 文件 | 页数 | 体积 | 内嵌图分辨率 | 角色 |
|---|---|---|---|---|
| `线代9讲强化.pdf` | 121 | 204 MB | 192 DPI | **主蓝本骨架** |
| `考研数学大纲.pdf` | 143 | 10 MB | 200 DPI（部分页 400 DPI） | **范围边界** |
| `高等代数 第五版 (…王萼芳 石生明).pdf` | 325 | 36 MB | 267–300 DPI | 深化材料·教材 |
| `高等代数典型问题与方法 (樊启斌).pdf` | 677 | 102 MB | 150 DPI | 深化材料·教辅 |

**四份全部是整页位图，每页恰 1 张图，`get_text()` 一律返回空。**
→ **不存在"抽文字层"这条捷径，所有内容只能靠渲染成图 + 视觉转录。**

### 3.2 考纲中线性代数的确切位置
`考研数学大纲.pdf` 的线性代数部分 = **PDF 第 18–20 页**（印刷页 14–16）。
- 页码换算：`pdf_page = 印刷页 + 4`
- 结构：`V 考试内容和考试要求` → `数学（一）`（PDF p10 起）→ 线性代数六节
- 之后 PDF p21 起进入「概率论与数理统计」，**线性代数到 p20 为止**

考纲线性代数全部六节（**这是唯一的范围边界**）：

| # | 节 | 考纲要求条数 |
|---|---|---|
| 一 | 行列式 | 2 |
| 二 | 矩阵 | 5 |
| 三 | 向量 | 8 |
| 四 | 线性方程组 | 5 |
| 五 | 矩阵的特征值和特征向量 | 3 |
| 六 | 二次型 | 3 |

> 注意：考纲**明确包含**"n 维向量空间、子空间、基底、维数、坐标""基变换和坐标变换公式、过渡矩阵"
> "内积、施密特正交化、规范正交基、正交矩阵"——这些常被误以为超纲，实际在范围内。

### 3.3 《线性代数9讲》完整结构（PDF p11 = 真目录）

| 讲 | 标题 | 印刷页 |
|---|---|---|
| 第1讲 | 行列式 | 1 |
| 第2讲 | 余子式和代数余子式的计算 | 10 |
| 第3讲 | 矩阵运算 | 14 |
| 第4讲 | 矩阵的秩 | 41 |
| 第5讲 | 线性方程组 | 49 |
| 第6讲 | 向量组 | 64 |
| 第7讲 | 特征值与特征向量 | 72 |
| 第8讲 | 相似理论 | 79 |
| 第9讲 | 二次型 | 93 |

书前 p2–p10 是前言与「OPD 三向解题法」（方法论，非知识点）。

### 3.4 教材/教辅的目录位置与页码偏移（已实测，勿重查）

| 源 | 目录（PDF 页） | 偏移换算 | 置信度 |
|---|---|---|---|
| `线代9讲强化.pdf` | p11 | `PDF 页 = 印刷页 + 11` | ✅ 实测（pdf11 目录 → pdf12 = 印刷 1） |
| `考研数学大纲.pdf` | p4 | `PDF 页 = 印刷页 + 4` | ✅ 实测（pdf9 页脚 = 印刷 5） |
| `高等代数 第五版…` | p10–p12 | **`PDF 页 = 印刷页 + 12`**（仅限印刷 1–163） | ⚠️ **分段偏移，见下** |
| `高等代数典型问题与方法…` | p6–p8 | `PDF 页 = 印刷页 + 9` | ✅ 实测（7 个抽样点吻合） |

### ⚠️⚠️ 北大《高等代数》第五版：偏移**不是常数**，且存在**两套页码范围**

实测页脚读数：

| PDF 页 | 页脚显示 | 所属 |
|---|---|---|
| 45 | **33** | 第二章（§3 n 阶行列式） |
| 51 | **39** | 第二章（§3 n 阶行列式） |
| 55 | **43** | 第二章（§5 行列式的计算） |
| 74 | **65** | 第一章 多项式（§1.7 思考与练习） |
| 175 | **163** | 第四章 矩阵 |

- **印刷页 1 – 163 区间（PDF 13 – 175）：偏移恒为 `PDF = 印刷页 + 12`。**
- **印刷 163 之后**：进入**「习题答案与提示」等附属部分，页码重新起算**，
  该公式**彻底失效**（例：总习题在印刷 289，但 163 + 页数 ≠ 198）。
  这一区间必须**逐页看页脚**确认，不要用任何偏移公式。
- 📌 **教训**：早期曾把北大偏移记为 `+9`，那是**未经核实的假设**（我误将子代理报告的页码
  当成页脚读数，从未真正看图）。**2026-xx 由子代理实测页脚推翻，已纠正为 +12。**
  引用北大页码前，务必按下条流程核验。

> **定位任意章节的可靠流程（强制）**：先用偏移算出候选 PDF 页 →
> **渲染该页页脚并目视读数确认** → 再决定正式渲染范围。
> **绝不允许**凭偏移公式直接批量渲染后就开始转录。这条规则对**所有**源文件适用。
>
> **樊书**：页码在**页面顶部**，但**章标题页顶部空白**（常规留白），
> 故"顶部空白"不能作为偏移不对的证据。实测点：PDF 74 顶部「§1.7 思考与练习 **065**」⟹ +9；
> 另 p364=355、p427=418、p435=426、p467=458、p555=546、p589=580、p604=595 全部吻合 +9。
> ⚠️ 樊书 PDF 第 4~5 页是**前言**，不是正文；第 1 章正文自 PDF p9 起。

### 3.4b 素材渲染状态（已全部闭合，勿重复渲染）

**9 讲各章页边界已实测确认**（`PDF = 印刷 + 11`）：

| 章 | PDF 页 | 章 | PDF 页 |
|---|---|---|---|
| 第 1 章 行列式 | 12–20 | 第 6 章 向量组 | 75–82 |
| 第 2 章 余子式 | 21–24 | 第 7 章 特征值 | 83–89 |
| 第 3 章 矩阵运算 | 25–51 | 第 8 章 相似理论 | 90–103 |
| 第 4 章 矩阵的秩 | 52–59 | 第 9 章 二次型 | 104–121 |
| 第 5 章 线性方程组 | 60–74 | | |

**✅ 全部 9 章 9 讲页面 + 北大第 3–9 章深化料 + 樊书第 3–9 章深化料均已渲染完毕**
（`_work/pages/` 共 **1420 张**图，含整页与 T/B 半页放大图）。
**后续各章不需要再渲染，直接进转录。**

### 3.5 两本高等代数书的章号体系（引用时必须用这一套）

- 北大《高等代数》第五版：10 章。**「相似理论」在第七章 线性变换**（§4 特征值与特征向量、§5 对角矩阵），
  **不在**第八章 λ-矩阵。第八章才是 λ-矩阵/Jordan 标准形理论推导。
  - §6 值域与核、§7 不变子空间、§8 Jordan 标准形介绍、§9 最小多项式（选学）
  - 第九章 欧几里得空间 **含考纲要求的内积/施密特正交化/规范正交基/正交矩阵（§2）与实对称矩阵标准形（§6）**，
    **不可因章名判为超纲**。
- 樊启斌：10 章。**「相似与可对角化」分散在 §6.4 与 §7.3**；最小多项式在 §7.2；
  Jordan 与幂零矩阵在 §7.5；实对称矩阵在 §8.4；正交矩阵在 §9.2。
- 引用格式约定：`北大 七.5（印刷页 203）`、`樊 7.2（印刷页 426）`。
  完整来源映射见 `OUTLINE.md` §14。

---

## 4. 工作流程规则

### 4.1 一章一章处理，不得跳章并行
用户明确要求逐章推进。每章固定流程：

```
1. 读 OUTLINE.md 中该章条目  →  确认本讲范围 + 待补充清单
2. 渲染源页面（整页 + 上下半页放大）
3. 视觉逐页转录（子代理并行，每片 5~8 页）
4. 整合补充深化知识（按第 2 节决定二的判定标准）
5. 自检编译 → 汇总构建
6. 验收（见 4.4）
7. 回写 OUTLINE.md 进度
```

**一章未验收通过，不得开始下一章。**

### ⚠️ 4.1b 并发写冲突（第 1、8 章各踩一次，必须遵守）
**子代理运行期间，主控不得编辑该子代理负责的文件。**

已发生两次，形态不同但根因相同——**把"文件存在 + 编译通过"误当成"子代理已结束"**：

| 章 | 主控做了什么 | 后果 |
|---|---|---|
| 第 1 章 | 改了 `ch01_b.tex` 的 OPD 标题 | 子代理判定"外部误改"，**按原文恢复**，覆盖了主控的修改 |
| 第 8 章 | 把裁决注释写进 `deep_ch08_beida.tex` | 该子代理**仍在重写文件**，注释被反复冲掉（实测 20 秒内 md5 变化），白做一次 |

**唯一可靠依据是 settle 通知。** 拿不到通知时，用文件稳定性做近似判定：
```bash
python3 _work/safe_write.py check <分片名> --wait 15   # 退出码 0 = 可安全写入
python3 _work/safe_write.py append <分片名> --note-file /tmp/note.txt
```
该脚本在 15 秒内多次取样 md5，**只要检测到变化就拒绝写入**（退出码 1）。

**推论**：文件"块数变化""大小波动""编译报错"都可能只是**写入中的瞬时状态**——
第 8 章的 `ch08_a` 曾报 `fcard` 环境不配平（6/10），子代理结束后复测为 **11/11 通过**；
`deep_ch07` 主控先后数到 9 块与 10 块，实际最终为 10 块。
**不要在子代理运行期间基于瞬时状态下判断或动文件。**
→ 子代理若发现文件被外部改动，应在报告中提出而不是自行"恢复"。

### 4.2 转录铁律
- **先探查再动手**；**绝不假设文字层可用**（本仓库已验证不可用）
- **转录靠看图，不靠抽文本**（`read` 看渲染图，公式必须看清上下标）
- **严格照搬源稿**：原稿的笔误、编号跳号、错别字一律保留，只在最终报告提示。
  要改必须先问用户。
- **每个转录分片必须自检编译**（wrapper：`\input{style.tex}` + `\input{frag/片}`），
  `exit=0` 且无 `!` 才算过。

### 4.3 技术约定
- 工具链：TeX Live (`xelatex`) + PyMuPDF。**本仓库无 poppler**（`pdftotext`/`pdfinfo` 不可用），
  一律用 PyMuPDF 替代。
- **坐标一律用页面比例 0~1**（`--region x0,y0,x1,y1`），与 DPI 无关。
- 圈码 ①②③④（U+2460+）在 xeCJK 下会**静默丢字**（编译不报错，字是空白）。
  `style.tex` 必须有 `newunicodechar` 全局兜底，换模板时不许漏。
- 不要用 `\newcommand` 定义各册会覆盖的宏（如页眉标记），用 `\providecommand`。
- **位图插图不会自己出现**：子代理只能转文字，收尾必须专门处理插图
  （TikZ 重绘，或 `crop_figures.py` 裁切原图）。
- 产物统一放 `_work/`，不污染仓库根目录。

### 4.3b LaTeX 静默失败模式（第 3 章踩到，务必避免）
1. **填空题的空格不要写 `___`**。裸下划线在文本模式下是数学模式字符，会报
   `! Missing $ inserted`。正确写法：`\underline{\hspace{2em}}`。
2. **`\underbrace{...}_{\text{中文}}` 会崩**：`\text{}` 在数学模式里遇中文 + 下标的组合易出错。
   改写法：把中文说明移到公式外面用正文叙述，公式本身只用纯数学符号。
3. **中文括号不要直接包裹数学式**：`（$x$）` 可以，但 `（$x$` 后紧跟数学内容再接中文括号
   容易出问题。稳妥做法是数学式与括号间留一个空格，且括号配对明确。
4. **`\fsec`/`\fcard` 标题里含数学公式时**，解析花括号必须用**配平计数**，
   不能用 `\{([^}]*)\}` 这类朴素正则（标题里的 `$a_{ij}$` 自带花括号）。第 1、2 章各踩一次。
5. **⚠️ `\fsec` 标题里含 `\cdots` 会编译失败**（第 6 章踩到）：hyperref 生成 PDF 书签字符串时
   报 `! Improper alphabetic constant`，`exit=1`。`\dots`/`\ldots` 与普通 `$Ax=0$` 不受影响。
   **两种修法**：① 标题内改用 `\dots`；② 用 `\fsec{\texorpdfstring{带 \cdots 的标题}{纯文本替代}}`。
6. **⚠️ `\addcontentsline` 前必须加 `\phantomsection`**（第 9 章收尾时发现，**读者可见的缺陷**）：
   `\fchap`/`\fsec` 是自定义命令、不是真 `\section`，若直接 `\addcontentsline`，
   hyperref 会把 PDF 书签**挂到上一个 tcolorbox 锚点**上（`.toc` 里表现为
   `\contentsline{section}{第2章…}{24}{tcb@cnt@fcard.15}` 这种锚点名），
   导致**点击书签跳到错误页**（实测第 1 章书签指向 p1，实际在 p5；第 7 章指向 p110，实际 p114）。
   **修复**：在 `\addcontentsline` 之前插入 `\phantomsection`。修后 11 个书签页码全部准确。
   **验收方法**：`python3 -c "import pymupdf;d=pymupdf.open('…pdf');print(d.get_toc())"`，
   逐个比对书签页与章标题**实际所在页**（注意标题里的 `\quad` 在文本提取时会变成空格，
   比对时**要先去掉空格**，否则会误判）。

> 编译自检必须**看 `^!` 与 Missing character 计数**；`exit=0` 但缺字、或警告被忽略，
> 都可能让成品静默出错。**绝不允许凭"看起来对"收工。**

### 4.3c 非 CJK 区的 Unicode 字符会静默丢字（第 3、6 章各踩一次）
`xeCJK` **只接管 CJK 区**的字符。以下字符区**不在其管辖范围**，会落进 `lmroman`，
而 lmroman 没有这些字形 → **编译不报错、字变空白**：

| 字符区 | 例子 | 状态 |
|---|---|---|
| 圈码 U+2460+ | ①②…⑳ | ✅ 已在 `style.tex` 兜底 ①–㉚ 共 30 个 |
| **罗马数字 U+2160+** | **Ⅰ Ⅱ Ⅲ Ⅳ Ⅴ Ⅵ** | ✅ 已兜底 Ⅰ–Ⅵ（第 6 章踩到） |
| 长箭头 | **⟹**（U+27F9）、⟸、⟺ | ✅ 已兜底（第 5 章踩到，当时改用 `\Longrightarrow`） |
| **文本模式的关系符** | **≠**（U+2260）、≤、≥、≈ | ⚠️ **文本模式里会缺字**（第 9 章踩到）。必须写成 `$\ne$`、`$\le$`、`$\ge$` |
| 数学字母数字 U+1D400+ | 𝑨 𝒙 等 | ⚠️ **未兜底，不要用**，一律写普通 `$A$`、`$x$` |

**规则**：
1. **优先用 LaTeX 命令或文字表述，而不是 Unicode 符号**。例如
   罗马数字写"第 1 组基/第 2 组基"、长箭头用 `\Longrightarrow`、蕴含用 `\Rightarrow`（⇒ 本身正常）。
2. 若确需使用上表中的符号，**先确认 `style.tex` 有兜底**；没有就补上或改写。
3. **新增任何非 ASCII 符号后，必须编译并检查 `Missing character` 计数为 0**——
   这是唯一能发现此类问题的手段。

### ⚠️ 4.3d 批量文本变换的三条铁律（第 9 章修缮时踩坑，代价很大）

修缮阶段用脚本改全库文本时，一次失误就损坏了 10 个成品文件。三条铁律：

**① 改全库前必须先备份。**
本仓库**没有 git**。修缮引号时我写了一个脚本，它悄悄删掉了**所有行内公式的 `$`**
（`$|A|=0$` → `|A|=0`），10 个文件的正文 `$` 全部归零，编译报 250 个错误、1790 个缺字。
仓库无 git、无 `.bak`，只能靠源分片回填（命中 96.2%），剩余 25 行手工重贴 `$`。
→ **凡是要动 3 个以上文件的脚本，先 `cp -r frag /tmp/frag_备份`。**

**② 用 `$` 切分再拼接时，必须显式保留 `$`。**
出错的写法：
```python
for m in re.finditer(r"\$", line):
    parts.append((math, line[i:m.start()])); i = m.end()   # ← $ 被吃掉了
```
`i = m.end()` 直接跳过 `$`，拼回去时 `$` 就没了。
→ **能用正则一步替换就不要手工切分**；必须切分时，把分隔符本身也 append 进去。

**③ 无参宏后面紧跟中文，会被当成控制序列名。**
xeCJK 下 CJK 字符属**字母类（category 11）**，所以 `\fbref服务考纲` 会被 TeX 解析为
名为 `fbref服务考纲` 的控制序列 → `! Undefined control sequence`。
→ **无参宏（如 `\fbref`）后面必须写 `{}`**：`\fbref{}服务考纲`。
   带参数的宏（`\key{}`、`\warn{}`）不受影响。

### ⚠️ 4.3e 规范文件自身的书写风格会被子代理模仿

`_work/TRANSCRIBE-SPEC.md` 是每个子代理**第一个要读**的文件。它当时用 Markdown 的
`**加粗**` 书写，子代理便把这个风格带进了 LaTeX 片段，成品 PDF 里出现 **63 处字面的 `**`**
（共 126 个星号），**编译期完全不报错**。
→ **规则文件必须用它要求下游使用的语法来书写。**
   已在 `TRANSCRIBE-SPEC.md` 顶部加入硬性规则：LaTeX 片段**禁止**任何 Markdown 语法
   （`**粗体**`、`*斜体*`、`# 标题`），加粗一律写 `\textbf{}`。

### 4.3f 排版体例（修缮阶段确立，后续一律沿用）

| 项目 | 规定 | 现状 |
|---|---|---|
| 中文引号 | **只用 `“”`**；禁止 ASCII `"`、禁止 `「」` | 809/809 开合相等，残留 0 |
| Markdown 语法 | **禁止**进入 `.tex`（`**`、`#` 等） | 残留 0 |
| 蓝 `\key{...}` | 「要记住名字」：定理/定义/性质名、方法名 | 43 处 |
| 红 `\warn{...}` | 「会做错」：易错点、陷阱、否定提醒 | 20 处 |
| 红 `\must{...}` | 「会做错」：必记结论、口诀、秒判 | 18 处 |
| 金 `\fbref{}` | `【反哺点】` 标签着色（**必须带 `{}`**） | 164 处 |
| ⚠️ 色彩施用边界 | **不得**用在 `\fsec`/`\fcard`/`\fkey`/`\fdeep` 的标题参数里（标题已有彩色底条，叠色会糊） | — |

> 强调宏定义在 `_work/style.tex`。**它们和 `fdeep`/圈码兜底一样，属于手写补丁，
> 跑 `init_project.py --force` 后必须补回**（见 §7.1）。

### 4.4 验收清单（每章都要过）
- [ ] 编译 `exit=0`，`grep '^!'` 为空
- [ ] `grep -c 'Missing character'` 为 0
- [ ] Overfull \hbox 全部 < 8pt
- [ ] 对照目录核对无漏转、无重复（尤其分片接缝页）
- [ ] 公式与插图对应关系正确
- [ ] 深化内容确实服务考纲知识点（抽查 `【深化】` 条目的"反哺点"是否成立）
- [ ] 每个 `fdeep` 块含 `【反哺点】`（机械核查：逐块检查，不要只全文 grep）

### 4.5 数值验证自身的可靠性（重要教训）

本项目对每条手写结论都做数值验证。**但"验证失败"未必等于"结论错"——也可能是验证方法错。**
实际已发生 **3 次**误判：

| 次数 | 现象 | 真实原因 |
|---|---|---|
| 1 | 秩-零化度定理"大量失败" | `svd(compute_uv=False)` 对 $m<n$ **只返回 $\min(m,n)$ 个奇异值**，零奇异值未被表示 |
| 2 | 特征子空间维数恒为 0 | 零空间判定容差取到 `eps` 量级，而含舍入误差的矩阵其"零奇异值"在 `1e-14` 量级 |
| 3 | 可对角化判据"失败" | 检验用例几乎全是可对角化矩阵，**没构造出反例**（Jordan 块），等于没测到 |

**因此，写验证脚本时必须遵守**：
1. **交叉验证**：同一结论用两种独立算法算（如 rank 与 nullity 同源计算，保证互补）。
2. **容差要与库函数同源**：用 `np.linalg.matrix_rank` 的容差逻辑，不要自己取 `eps`。
3. **必须构造反例**：测"当且仅当"类结论时，**两类用例都要有**（可对角化 / 不可对角化；
   满秩 / 不满秩），否则可能全部落在同一侧而"通过"。
4. 记录"该数值结论已被何种测试覆盖"，便于日后复查。

> 与之配套的心态：**子代理报告的"发现"也要验证**。第 2 章曾有一位子代理举报原书公式有误，
> 主控复核后发现是**它自己的反例算错了**（`det=6−4=2` 写成 6），书中公式反而正确。

---

## 5. 目录结构约定

```
高等代数/                      ← 仓库根
├── AGENTS.md                  ← 本文件：规则
├── OUTLINE.md                 ← 知识补充大纲 + 进度台账（活文档，随进度更新）
├── 参考/                       ← 源 PDF（保持不动；不要改名！脚本按名字引用）
└── _work/                     ← LaTeX 工程
    ├── style.tex              ← 设计系统（由 config.json 生成）
    ├── 主控 .tex               ← 主控文档
    ├── frag/                  ← 逐片转录内容（章-片.tex）
    ├── fig/                   ← 插图（裁切原图 或 TikZ）
    ├── pages/                 ← 渲染出的页面图（中间产物，勿提交）
    └── build/                 ← 编译产物 + 成品 PDF
```
> 若迁移源 PDF 到 `参考/`，必须先确认引用路径已同步更新，否则脚本会找不到文件。
> **当前状态：源 PDF 仍在仓库根目录，尚未迁移。**

---

## 6. 进度台账

见 `OUTLINE.md` 末尾「进度」一节。每完成一章必须更新。

---

## 7. 已定 / 待定事项

### 7.1 已定（用户确认，不得擅自更改）

| # | 事项 | 决定 |
|---|---|---|
| 1 | 成品分册方式 | **单册**通排（不分「基础篇/强化篇」） |
| 2 | 9 讲的「OPD 三向解题法」方法论 | **不保留**。蓝本只收知识点与解法，不收方法论章节 |
| 3 | 源 PDF 位置 | **允许迁移**到 `参考/` 子目录（迁移时须同步更新引用路径） |
| 4 | 例题取舍粒度 | **例题精选**，剔除纯重复练习（见决定一） |
| 5 | 「深化」内容的版式 | **灰底框**（`fnote` 同族的灰底拓展框）就近嵌入正文，**不摆到章末附录** |
| 6 | **许可协议** | **CC BY-NC-SA 4.0**（单一协议覆盖全仓库）。见 `LICENSE`；**须同时保留来源材料与版权归属声明**：本作品是衍生整理，四份来源材料的著作权归其作者/出版者，**不在授权范围内**；本协议仅覆盖作者独立完成部分（排版代码、设计系统、脚本、取舍组织、深化撰写、图表重绘） |

> 第 5 条的落地：深化内容必须紧贴它服务的考点出现，用灰底框与正文视觉区分。
> 已实现为 `_work/style.tex` 里的 `\begin{fdeep}{标题}` 环境（灰底 + 左侧灰条 + 标题条）。
>
> ⚠️ **重要**：以下四类是**手写补丁**，**不在 `style.tex.tmpl` 里**。若跑 `init_project.py --force`
> 重新生成 `style.tex`，**必须把它们全部补回**（搜 `⚠️` 可定位补丁区）：
>
> | 补丁 | 缺失后果 |
> |---|---|
> | `fdeep` 灰底拓展框环境 | `\begin{fdeep}` 报 **undefined environment** |
> | 圈码兜底 ①–㉚（30 个） | 圈码**静默丢字**（编译不报错，字空白） |
> | 罗马数字兜底 Ⅰ–Ⅵ + 长箭头 ⟹⟸⟺ | 同上，**静默丢字** |
> | `\iddots`（副对角线反向省略号） | 报 **Undefined control sequence** |
>
> **`\iddots` 的用途**：副对角线分块矩阵/行列式。`mathtools` **不含**该宏；
> 若不定义，转录代理会用 `\ddots` 顶替 —— 那是**主对角线方向**、**数学上错误**。
> 第 3 章已出现此问题（副对角线分块矩阵求逆），已修正。凡遇副对角线省略号一律用 `\iddots`。
>
> ### ⚠️ OPD 标记的**统一处理口径**（必须在每个转录 prompt 里写明）
> 第 7 章曾出现体例不一致：子代理把节标题里的 OPD 词组"翻译"成中文后**保留**了
> （`（常规操作＋转换等价表述＋化归经典形式）`），而第 1–6 章是**整块删除**。
>
> **唯一正确口径**：
> 1. `\fsec`/`\fcard`/`\fkey` 标题里的 OPD 括号 —— **连同其中的词组一起整段删除**。
>    `（$D_1$（常规操作）+$D_{22}$（转换等价表述））` → 删掉整个括号；
>    **"常规操作""转换等价表述""化归经典形式""观察研究对象""盯住目标""脱胎换骨"
>    这些词本身就是 OPD 术语，译为中文也属方法论，同样要删**。
>    标题里**只保留数学方法名**（如「一、用行列式」「三、利用矩阵方程命题」）。
> 2. 正文里的 `→$D_{xx}$（…）` 旁注 → 不排入正文，以 `% OPD 蓝色标注（原稿）：…` 留痕。
> 3. 页首「三向解题法」思维导图 → 只留 `% FIGURE-OPD: <印刷页> …（按规则不保留）`。
> 4. 页边竖排模块名（「隐含条件体系块」「等价表述体系块」「形式化归体系块」）→ 删除并留痕。
> 5. **数学内容不受影响**：$D$、$D_n$、$P$、$\lambda$ 等作为数学对象出现时照抄。

### 7.2 待定

| # | 事项 | 状态 |
|---|---|---|
| 7 | 习题/练习是否收录 | ✅ **已定：不收录**（考纲无要求；第 1 讲实际无习题栏，已核实到 PDF p20 结束） |

> 第 1 章已按 §4.1 流程完整走完并通过 §4.4 验收，施工记录见 `OUTLINE.md` §13。
